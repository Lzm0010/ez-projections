{
  "name": "ez-projections",
  "version": "1.0.0",
  "description": "easy projections",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha server/**/*.test.js",
    "test-watch": "nodemon --exec 'npm test'"
  },
  "author": "Lee McCormick",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "mongoose": "^4.10.0"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.4.1",
    "nodemon": "^1.11.0",
    "supertest": "^3.0.0"
  }
}
